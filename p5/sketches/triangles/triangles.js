/* triangles.js */

var mixed = mixedPalette();
var triangles = [];

function setup() {
    var canvas = createCanvas(640, 640);
    canvas.parent('triangles-holder');

    setupTriangles();
}

function draw() {
    for(let i = 0; i < 10; i++) {
    }
}


